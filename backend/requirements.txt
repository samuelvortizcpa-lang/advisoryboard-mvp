fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.0
pydantic-settings==2.6.0
python-dotenv==1.0.1
sqlalchemy==2.0.36
alembic==1.14.0
httpx==0.27.2
pytest==8.3.3
pytest-asyncio==0.24.0
PyJWT==2.9.0
cryptography==43.0.3
psycopg2-binary==2.9.10
pgvector==0.3.6
email-validator==2.2.0
python-multipart==0.0.20

# RAG pipeline
openai>=1.54.0
pdfplumber>=0.11.4
python-docx>=1.1.2
extract-msg==0.48.0

# Anthropic (Claude — strategic query routing)
anthropic>=0.42.0

# Chat export
reportlab==4.0.9

# S3 file storage (optional — falls back to local filesystem when credentials not set)
boto3>=1.35.0

# Supabase Storage
supabase>=2.0.0

# Google APIs (Gmail integration)
google-api-python-client>=2.100.0
google-auth>=2.23.0

# Multimodal RAG (Gemini page-image embeddings)
google-genai>=1.0.0
pdf2image>=1.17.0
pytesseract

# Stripe payments
stripe>=8.0.0

# Microsoft OAuth (Outlook integration)
msal>=1.28.0

# Error monitoring
sentry-sdk[fastapi]>=2.0.0

# Scheduled auto-sync
apscheduler>=3.10.0
